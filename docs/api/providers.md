# Providers API

There is no public provider API in the current codebase.

## Current state

- No provider manager
- No provider registry
- No provider selection options
- No fallback router

## What to document instead

If provider abstractions are added later, this page should be updated to describe the real public classes and methods. For now, `Shiprocket` is the only integration.
